# Runtime dependencies for the Lambda bundle.
# boto3 is intentionally omitted: the AWS Lambda Python runtime provides it.
# For local development use `pip install -e .[dev]` instead.

aiohttp>=3.10
fastapi>=0.115
mangum>=0.19
pydantic-settings>=2.6
